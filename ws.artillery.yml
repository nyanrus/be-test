config:
  target: 'ws://localhost:80/api/v1'
  phases:
    - duration: '60s'
      arrivalRate: 10
      rampTo: 200

scenarios:
  - name: My WebSocket test
    engine: ws # Enable the Socket.io engine
    flow:
      - send: 'Hello world!'